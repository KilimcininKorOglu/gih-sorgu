version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: gih-sorgu
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    main: .
    binary: gih-sorgu
    ldflags:
      - -s -w -X 'main.Version={{.Version}}' -X 'main.BuildCommit={{.Commit}}' -X 'main.BuildTime={{.Date}}'

archives:
  - id: gih-sorgu
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - LICENSE
      - README.md

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
